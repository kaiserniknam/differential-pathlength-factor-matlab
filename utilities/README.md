# Utilities

`breakyaxis.m` was present in the supplied archive. Its source, authorship, and
license were not identified in the file header. It is retained for private
research use but must not be redistributed publicly until provenance and
licensing are confirmed.

The missing instrument functions `do_FDS100`, `do_LED750L`, and `do_LED850LN`
belong under `utilities/characteristics/` when supplied.
